# 米糕记账 v1.5 视觉与交互 QA

## 对照素材

- 视觉源：`C:\Users\50699\AppData\Local\Temp\codex-clipboard-e0761d85-c402-4619-99da-b9f4f12356d8.png`
- 最终首页：`D:\越\ui-audit\16-v15-final-home.png`
- 最终记一笔：`D:\越\ui-audit\17-v15-final-add.png`
- 最终统计：`D:\越\ui-audit\18-v15-final-stats.png`
- 有账单状态：`D:\越\ui-audit\14-v15-home-seeded.png`
- 最终并排对照：`D:\越\ui-audit\21-comparison-final.png`
- 顶部重点对照：`D:\越\ui-audit\22-comparison-final-focus.png`
- 离线重载：`D:\越\ui-audit\20-v15-offline-reload.png`
- 视口：390 × 844，iPhone 触控与移动端 UA
- 状态：首页空账单、首页三笔演示账单、快速记一笔、统计趋势、完全断网重载

## 最终检查

### 字体与层级

- 标题、摘要、金额、辅助文字层级清晰；中文使用系统字体，不增加在线字体依赖。
- 深墨绿标题和加深后的灰绿小字在近白卡片上清楚稳定。
- 支出和收入金额除颜色外仍有正负号及文字语义，不只依赖颜色。

### 间距与布局

- 首页宽度严格为 390px，`scrollWidth === innerWidth`，没有横向滚动或被裁切控件。
- 月度摘要、独立“记一笔”按钮、今日记录和固定导航的节奏与参考图一致。
- 米糕维持较小尺寸，叠在摘要卡右上角，不遮挡标题、金额或主按钮。
- 空状态的重要文字及今日记录底部操作均显示在固定导航上方；更多内容可自然纵向滚动。

### 颜色与背景

- 背景使用本地水彩 WebP 纹理，而不是单一纯色；边缘有轻微薄荷纹路，中间内容区保持低对比。
- 卡片改为单层浅边框和轻阴影，移除了旧版内层虚线。
- 主色、支出色、正文色和辅助色已统一到清透薄荷体系。

### 图片与图标

- 米糕默认图和 wink 图均完整加载，透明边缘和阴影正常。
- 底部导航、摘要、分类、日历和操作图标统一使用本地 Tabler Icons SVG，不再依赖系统表情渲染。
- 首页、弹层和离线重载中所有图片 `complete === true` 且 `naturalWidth > 0`。

### 文案与功能

- 首页名称仍为“米糕记账”，未增加提醒铃铛。
- 记账顺序保持金额 → 备注 → 分类；分类仍按使用次数优先，基础支出分类为 28 个。
- 两位小数保存测试通过：输入 `12.34` 后保存为 `1234` 分。
- 米糕点击只切换互动状态，不替换页面节点，页面透明度始终为 1，没有整页闪屏。
- 账单、统计导航正常；统计末尾日期标签不再发生 30/31 重叠。
- 离线缓存版本升级后，在停止本地服务的情况下仍能完整加载背景、米糕、图标和页面。

## 对照迭代历史

1. Pass 1：发现月度摘要卡比参考图高约 60px，挤压今日记录首屏空间（P1）。
   - 修正：缩短摘要标题、指标图标和底部记录行的垂直间距，并把主按钮保持为卡外独立操作。
2. Pass 2：空状态底部操作靠近固定导航，视觉上略显拥挤（P2）。
   - 修正：继续压缩摘要卡和主按钮高度，今日记录完整操作移动到导航上方。
3. Pass 3：统计图 30 日与 31 日轴标签相邻重叠（P2）。
   - 修正：月末不足两个日期间隔时不重复绘制最后一个轴标签。
4. 最终复核：并排全屏与顶部重点对照无剩余 P0/P1/P2；不加入铃铛、米糕较小和保留当前日期属于已确认的产品约束。

## 交互验证

- 米糕互动：通过
- 打开快速记账：通过
- 金额 → 备注 → 分类顺序：通过
- 分类数量与图标加载：通过
- 44 × 44px 关闭触控区：通过
- 保存记录并关闭弹层：通过
- 账单、统计导航：通过
- 390px 无横向溢出：通过
- 完全断网重载：通过

## 后续 P3

- 可在真实 iPhone 主屏幕 Web App 中再观察一次状态栏安全区和系统字体放大效果；不阻塞本版发布。

final result: passed
